# Local development only. Copy to .env.media-dev; never reuse these values in a shared environment.
POSTGRES_DB=player_profile
POSTGRES_USER=postgres
POSTGRES_PASSWORD=volputas-local-db-only
MINIO_ROOT_USER=volputas-local
MINIO_ROOT_PASSWORD=volputas-local-object-storage-only
MEDIA_S3_BUCKET=volputas-media
VOLUPTAS_PSEUDO_ID_SECRET=volputas-local-pseudonym-secret-only
